const chalk = require("chalk");

module.exports = (client, Discord) => {
  console.log(chalk.blue("[READY]") + " Jeeff is ready!");
  // client.user.setActivity("Jeeff | !help", { type: "PLAYING" });
};
